use std::collections::HashMap;

pub fn get_solutions() -> HashMap<u8, fn(String) -> ()> {
    let mut solutions: HashMap<u8, fn(String) -> ()> = HashMap::new();
    // solutions.insert(N, dN::run);
    solutions
}
